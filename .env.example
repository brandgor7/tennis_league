SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
# Absolute path for log files; defaults to logs/ inside the project root
# LOG_DIR=/var/log/tennis-scores

# WhatsApp group score import (Green API — free tier)
# Sign up at https://green-api.com, create an instance, scan QR with your WhatsApp.
# Find your group chat ID via Settings > Notifications or the getContacts API endpoint.
GREEN_API_INSTANCE_ID=
GREEN_API_TOKEN=
WHATSAPP_GROUP_ID=
